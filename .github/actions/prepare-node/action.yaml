name: Prepare Node environment
description: Set Node environment and cache
runs:
  using: composite
  steps:
    - uses: pnpm/action-setup@v4
      with:
        run_install: false

    - uses: actions/setup-node@v4
      with:
        node-version: 22.13.0
        cache: pnpm



